{
  "version": "1.6.0",
  "download_url": "https://github.com/salma383/myva-call-analyzer/releases/latest/download/CallAnalyzer.exe",
  "release_notes": "Smarter analysis. Audio chunks now split on silent gaps (cleaner transcripts at the seams). Scoring output is enforced by a strict JSON schema so it never fails to parse. Added a two-pass flow: a fact-extraction pass runs before scoring so the lead template is filled from facts the prospect literally stated rather than the model's guesses — significantly less hallucination."
}
