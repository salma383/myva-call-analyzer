{
  "version": "1.6.2",
  "download_url": "https://github.com/salma383/myva-call-analyzer/releases/latest/download/CallAnalyzer.exe",
  "release_notes": "Stability fix. Replaced OpenAI strict JSON schema mode (which caused the app to hang in 1.6.0–1.6.1) with the simpler json_object mode. Added explicit timeouts on every API call so the pipeline can never get stuck forever. Fact-extraction and scoring still run in parallel."
}
